-- ============================================
-- WIMC 마이그레이션 5 — 옷 스타일 키워드
-- 적용: Supabase 대시보드 > SQL Editor 에 실행 (1회)
-- 옷마다 스타일 키워드(미니멀/캐주얼 등)를 붙여, 분석에서 '실제 입은 옷 기반 키워드'를 집계한다.
-- (마이페이지의 '선호 키워드'와 대비)
-- ============================================
alter table items add column if not exists keywords text[] not null default '{}';
